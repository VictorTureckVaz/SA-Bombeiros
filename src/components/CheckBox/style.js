import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

      CheckBox: {
        width: 25,
        height: 25,
        borderColor: "#555555",
        borderWidth: 2,
        borderRadius: 2,
      },

      OptionContainer: {
        flexDirection: "row",
        gap: 5,
        alignItems: "center",
      },

      Text: {
        fontSize: 20,
      },

  });

  export default styles;