import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
      BigTextTitle: {
        fontSize: 40,
        fontWeight: 'bold',
      },
      SmallTextTitle: {
        fontSize: 16,
      },
  });

  export default styles;